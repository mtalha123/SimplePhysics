// following code will be added to the end of the library file using the 'wrap' property of the build file

    //make the SimplePhysics module (which is the entry point to the library) accessible to the public
    return require('SimplePhysics');
}));